import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.afaiktechnologies.cercafutur',
  appName: 'cercafutur',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
